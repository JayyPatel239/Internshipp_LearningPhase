friends =["Apple", "Banana", "Cherry", "Orange", "Grapes"]

print(friends[0])

friends[2] = "Mango"
print(friends[2])

#list are mutable wheareas strings are immutable we can replace the value of a list
# but we cannot replace the value of a string
#any datatype can be added in a list

print(friends[1:4]) # slicing nlist
