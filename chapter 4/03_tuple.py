#tuple is a collection but immutable any datatype can be added in a tuple
a = (1, 2, 3, 4, 5)
print(type(a)) # print the tuple
b=(1,) # for printing single element in tuple we need to add a comma
print(type(b)) # print the tuple